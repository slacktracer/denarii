{
  "dependencies": {
    "husky": "^8.0.1",
    "lint-staged": "^13.0.3"
  },
  "name": "denarii",
  "scripts": {
    "build": "chmod +x ./build.sh && ./build.sh",
    "d": "npm run decompile --workspace main",
    "execute": "node ./build/main.js",
    "lint-staged": "lint-staged",
    "prepare": "husky install",
    "start": "npm start --workspace main",
    "test": "npm test --workspace tests",
    "x": "npm run execute"
  },
  "workspaces": [
    "main",
    "tests"
  ]
}
