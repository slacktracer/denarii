version: '2.1'

jobs:
  test:
    docker:
      - image: cimg/node:latest
    working_directory: ~/repo
    steps:
      - checkout
      - run: npm ci
      - run: npm run build -w main
      - run: npm run test-once

workflows:
  version: 2
  provisioning:
    jobs:
      - test